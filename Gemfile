source 'https://rubygems.org'

ruby '>=3.1'

gem 'httparty', '~> 0.22'
gem 'colorize', '~> 1.1'

# multi_xml (via httparty) requires bigdecimal (~> 3.1); declare it so Bundler
# resolves it even when the dependency is missing from RubyGems dependency API metadata.
gem 'bigdecimal', '~> 3.1'
