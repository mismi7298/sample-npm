source 'https://rubygems.org'

ruby '>=3.1'

gem 'httparty', '~> 0.22'
gem 'colorize', '~> 1.1'

# multi_xml (via httparty) needs bigdecimal (~> 3.1). Rubygems'
# dependency index sometimes hides extracted-stdlib deps, so pinning
# avoids "revealed dependencies not in the API or the lockfile".
gem 'bigdecimal', '~> 3.1'
